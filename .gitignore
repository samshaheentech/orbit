# Runtime dirs — not committed, created by install.sh
running/
done/
blocked/
logs/
state/
worktrees/
reports/
briefs/data/

# Personal documents: the resume carries a phone number and the repo is public.
# They travel in the zip and live in the working tree, never in git. Delete these
# two lines to commit them.
config/lanes/career/resume/champion.md
config/lanes/career/resume/approved/

# OS
.DS_Store

# Python
__pycache__/
*.pyc

# Node (for future CLI packaging)
node_modules/
*.lock
