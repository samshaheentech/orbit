# Backlog

One-shot tasks Orbit pulls into the queue when a window has budget left and the queue is empty. Filename order is priority. The Sunday retro restocks this folder; you can too (`orbit add` then move the file here).

A backlog task is an ordinary task file. It runs once and lands in `done/`; it does not copy itself back.
