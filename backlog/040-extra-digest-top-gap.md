---
lane: research
model: sonnet-5
effort: medium
max_turns: 40
dir: ~/git/orbit
---
# Extra digest on the top gap

## Goal
One more digest, on whichever `profile` gap has the highest-priority topic in `state/research/topics.json` and no completed check. Same format and rules as `queue/300-research-digest.md`. If no gap exists yet, take the highest-priority unwritten topic.

## Steps
Follow `queue/300-research-digest.md` steps 2 to 6, then the STATUS block. Do not re-queue.
